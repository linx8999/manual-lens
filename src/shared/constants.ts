export const APP_NAME = "STM32 手册智能体";
export const APP_ID = "com.local.stm32rag";
export const APP_VERSION = "0.2.5";

/**
 * Folder created inside the user's documents directory when nothing else has
 * been configured. The knowledge base is always resolved at runtime so the app
 * works on any machine instead of pointing at the author's disk layout.
 */
export const DATA_FOLDER_NAME = "STM32RAG知识库";
